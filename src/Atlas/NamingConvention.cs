namespace Atlas;

/// <summary>
/// Naming style applied when matching members across a (source, destination) type pair.
/// </summary>
public enum NamingConvention
{
    PascalCase,
    CamelCase,
    SnakeCase,
}
